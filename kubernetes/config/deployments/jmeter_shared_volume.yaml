apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jmeter-shared-disk
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: jmeter-shared-disk-sc
  resources:
    requests:
      storage: 1Gi