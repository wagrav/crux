parameters:
  timeoutInMinutes: 5
steps:
  - bash: |
      echo "hook mock"
    condition: and(succeeded(), not(eq(variables.pre_hook, false)))
    displayName: User Hook
    timeoutInMinutes: ${{ parameters.timeoutInMinutes }}
